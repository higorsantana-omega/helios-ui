import { Button } from '@helios-ui/react'

const meta = {
  title: 'Button',
  component: Button,
}

export default meta

export const Primary = {
  args: {
    primary: true,
    label: 'Button',
  },
}
